name: Demo GitHub Actions Events
run-name: What is run-name?

on:
  push:
    branches:
      - main
      - feature/*
  pull_request:
    branches: [main]
  issues:
    types:
      - opened
      - closed
jobs:
  Greeting:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Good morning!"
    
